import { navLinks } from "@/constants"

export default function Navbar(){
    return (
        <nav>
            <div>
                navbar

                <ul>
                    
                </ul>
            </div>
        </nav>
    )
}